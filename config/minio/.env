# MinIO Environment Configuration
# Production-ready settings for OpenRepo

# Root credentials (change these in production!)
MINIO_ROOT_USER=openrepo_minio_admin
MINIO_ROOT_PASSWORD=OpenRepo_MinIO_2024_Secure!

# Console settings
MINIO_BROWSER_REDIRECT_URL=http://localhost:9001
MINIO_SERVER_URL=http://localhost:9000

# Security settings
MINIO_STORAGE_CLASS_STANDARD=EC:2

# Performance settings
MINIO_SCANNER_SPEED=default
MINIO_API_REQUESTS_MAX=10000

# Logging
MINIO_LOGGER_WEBHOOK_ENABLE=off
MINIO_AUDIT_LOGGER_HTTP_ENABLE=off

# Compression
MINIO_COMPRESS=true
MINIO_COMPRESS_EXTENSIONS=.txt,.log,.csv,.json
MINIO_COMPRESS_MIME_TYPES=text/*,application/json,application/xml

# Cache settings (for large deployments)
MINIO_CACHE=off
